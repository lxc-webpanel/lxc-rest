---
language: python
python: "3.5"

sudo: required
dist: trusty

before_install:
  - add-apt-repository ppa:ubuntu-lxc/stable
  - apt-get update
  - apt-get install lxc lxc-dev python3-lxc

addons:
  apt:
    packages:
    - python-dev
    - build-essential

script:
  - pip install git+https://github.com/lxc/lxc.git#subdirectory=src/python-lxc
  - python3 install/setup.py
